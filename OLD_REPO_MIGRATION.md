# Repository Consolidation Notice

The standalone rootless syslog-ng repository has been **consolidated** into the unified SentinelOne Syslog Solutions Toolkit.

The container images (`syslog` and `alpine-nc`) remain available and are now built from this repository via the `.github/workflows/build-rootless.yml` pipeline.
